// 1-19 min

// we evaluate our node code by using two options:

// 1)  REPL (**read–eval–print loop**)

// 2) CLI (**command line interface**)

const amount = 8
if(amount < 10) {
    console.log('small number')
} else {
    console.log('large number')
}

// open powershell terminal write cd and drag folder in it then write node + file name eg app.js then output will be printed

// control + ` to open vs terminal
// write node app.js in vs terminal to see same output

// 1-19 min

